export namespace CommonConstants {
  /* eslint-disable */ // why disable? coz its giving unused var error even though its used in other file
  export enum SlashCommandTypes {
    SUBSCRIBE = 'subscribe',
    UNSUBSCRIBE = 'unsubscribe',
  }
  /* eslint-enable */
}
